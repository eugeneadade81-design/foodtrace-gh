# FoodTrace GH environment template
# Copy this file to .env and replace the placeholder values before running locally.
#
# Production notes:
# - DATABASE_URL may be a JDBC URL or a hosted postgres:// URL.
# - JWT_SECRET must be set manually to a long random value.
# - PORT is provided by Railway at runtime; keep the local fallback below for development.

# PostgreSQL connection string.
# Local Spring example: jdbc:postgresql://localhost:5432/foodtrace
DATABASE_URL=jdbc:postgresql://localhost:5432/foodtrace

# JWT signing secret for access tokens. Use at least 32 random characters in production.
JWT_SECRET=replace-with-a-random-32-character-secret

# Backend server port and environment.
PORT=3000

# Allowed browser origins for CORS. Include both localhost and 127.0.0.1 during local demos.
FRONTEND_URL=http://localhost:5173,http://127.0.0.1:5173

# Allowed mobile deep-link or Expo origins.
MOBILE_ORIGINS=exp://localhost:8081,myapp://

# Demo-only: set true if you need /api/auth/request-otp to return the OTP in the response.
EXPOSE_OTP=false

# Africa's Talking SMS/USSD credentials.
# Get these from https://africastalking.com after creating an app.
AFRICASTALKING_API_KEY=
AFRICASTALKING_USERNAME=sandbox
AFRICASTALKING_SHORTCODE=

# Google Cloud Text-to-Speech.
# GOOGLE_APPLICATION_CREDENTIALS should point to a local service-account JSON file.
GOOGLE_APPLICATION_CREDENTIALS=
GOOGLE_CLOUD_PROJECT=

# Optional S3-compatible QR/image storage.
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_REGION=eu-west-1
AWS_BUCKET_NAME=foodtrace-gh-prod

# Optional public base URLs used in QR generation and callbacks.
PUBLIC_WEB_URL=http://localhost:5173
PUBLIC_API_URL=http://localhost:3000

# Production deployment URLs.
# Example after Railway deploy:
# PUBLIC_API_URL=https://your-foodtrace-backend.up.railway.app
# FRONTEND_URL=https://your-foodtrace-web.example.com

# Web presentation shortcuts. Keep false for real pilot and production builds.
VITE_SHOW_DEMO_MODE=false

# Medicine/pharmacy module. Keep false for the first food-traceability pilot.
VITE_ENABLE_DRUG_MODULE=false
