# Copy to .env for local dev. Base URL of the FoodTrace analytics/recall backend.
# Local Spring Boot service runs on 8081; on AWS Amplify (Day 12) set this in the
# Amplify environment to the deployed Elastic Beanstalk URL.
VITE_API_BASE_URL=http://localhost:8081
