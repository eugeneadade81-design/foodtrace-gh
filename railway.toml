[build]
builder = "NIXPACKS"
buildCommand = "cd backend && mvn -q package -DskipTests"

[deploy]
startCommand = "java -jar backend/target/foodtrace-api-0.1.0.jar"
healthcheckPath = "/actuator/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
